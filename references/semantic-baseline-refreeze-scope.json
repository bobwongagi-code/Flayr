{
  "schema_version": 1,
  "name": "semantic-baseline-refreeze-after-stage1-binding-normalization-2026-09-03",
  "description": "Stage1 qualification only: nested signal bindings are deterministically narrowed to the same stage's authoritative top-level evidence_ids; top-level evidence is never expanded, raw provider artifacts remain unchanged, and a supported binding emptied by normalization fails closed for only that stage. Qualification scale, gap rules, prompts, and model routing are unchanged.",
  "change_scope": {
    "max_files": 3,
    "max_added_lines": 40,
    "max_deleted_lines": 20,
    "allowed_globs": [
      "references/semantic-baseline-refreeze-scope.json",
      "references/semantic-baseline-freeze.json",
      "references/semantic-baseline-freeze.md"
    ],
    "forbidden_globs": [
      "runs/**",
      "outputs/**",
      "jobs*.json",
      "scripts/**/__pycache__/**"
    ]
  }
}
