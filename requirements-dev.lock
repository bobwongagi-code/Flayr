# Flayr development/CI lock file.
# Runtime analysis uses Python's standard library plus external media tools.
# Pillow is optional at runtime, but pinned here so evidence-rendering tests
# use a reproducible implementation in development and CI.
Pillow==12.3.0
ruff==0.16.0
black==26.5.1
mypy==2.3.0
bandit==1.9.4
coverage==7.15.2
pip-audit==2.10.1
