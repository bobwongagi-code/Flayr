# 运行产物（每次分析的输出，体积大、可重新生成）
runs/

# 开发临时产物
**/dev_stage2_*.json

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# 本地配置（可能含密钥/路径）
.claude/settings.local.json

# baseline-v3 运行数据(基准/实验输出,文件系统只读纪律,不入 git)
runs_baseline_v3/
runs_exp/
