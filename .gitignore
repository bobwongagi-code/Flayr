# 运行产物（每次分析的输出，体积大、可重新生成）
runs/

# 开发临时产物
**/dev_stage2_*.json

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
build/
dist/
*.egg-info/

# 本地配置（可能含密钥/路径）
.claude/settings.local.json

# 本地批处理/验证清单可能包含绝对媒体路径
jobs.local.json
jobs.json
jobs.*.json
jobs-*.json
runs_tmp_*-jobs.json
references/validation-inputs.local.json
references/ground-truth-labels.local.json
