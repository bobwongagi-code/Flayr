# Flayr 待办与决策记录

> 来源：2026-06-09 一轮分析链梳理 session。集中沉淀延后项 + 已否决提案，
> 便于将来写 spec §0 时统一收编，也避免重复提已否决的方案。

## 本轮新增基建（已落地，2026-06-09）

为支撑"大量视频处理 + 实验"，已建：

- **传输层流式**：`api.py:call_llm_api` 改 SSE 流式（curl `--http1.1 --no-buffer`），分块拼装 +
  `[DONE]`/`finish_reason` 判完整 + 截断整次重试。根治大响应在网络层被静默截断（非流式下
  6000+ token 响应近乎确定性截断）。下游 `fetch_json_completion` 再加一层"内容不可解析就重取"。
- **预处理复用**：`flayr.py --reuse-preprocessing`，复用 `_preprocess.json` 跳过抽帧/转写/OCR。
  实验迭代（同视频改 prompt/代码）和 LLM 失败补跑直接进 LLM 步。
- **脱离式批处理 runner**：`scripts/batch_analyze.py`，nohup 起一次不被 harness cull；
  状态写 `runs/_batch/status.json`（廉价轮询）；断点续跑（跳过已 done）；故障隔离；限并发。
  用法：`nohup python3 scripts/batch_analyze.py jobs.json --concurrency 1 >runs/_batch/runner.log 2>&1 &`
  作业清单模板见 `runs/_jobs_rootify.json`。

> 环境教训：harness 跟踪的后台任务会 cull 长时/无输出进程；长批量作业必须走 nohup 脱离 + 状态轮询。

## 待办（按触发条件）

### 1. stabilize_stage_severity 整体去过拟合（大）
现状：S2/S3/S4/S5 的稳定化规则全是对着 runs 里唯一一条儿童牙膏视频长出来的正则关键词
（`creator_has_functional_effect`=按压/用量、`mentions_sensory_gap`=闻/香/口味、
`has_real_endorsement` 的否定处理等）。换品类大概率失效。
方向：把这些语义判断从正则改为**模型输出的结构化标记**（同 `product_visible` 的做法）——
模型按框架定义逐 stage 标 task_completion / 是否有外部背书 / 功能是否达成，代码只做确定性消费。
触发：集中重构时做，别零敲。

**回归集已就绪（2026-06-09 跑出 3 个跨品类真实样本）**，过拟合实锤：

| 样本 | 品类 | 牙膏正则命中 | 判定 |
|---|---|---|---|
| `runs/sample-are_xie` | 保健品（生理期营养） | 无 | ✅ 无关键词重叠→不触发→干净 |
| `runs/sample-kakwanreview` | 马桶刷（清洁工具） | 按压/用量/闻香/口味 | ❌ **误触发**：马桶刷"按压按钮"撞 `按压` 正则，灌入对马桶刷无意义的牙膏文案 |
| `runs/sample-tashadiyana` | 儿童牙膏 | 按压/用量/防蛀/闻香/口味/牙膏 | ✅ 同品类→正确触发 |

结论：`creator_has_usage_demo` 等靠关键词巧合，在③同品类对、①无重叠品类对、但②**碰巧共享关键词的品类误触发**。重构用这 3 个样本当回归集验证。

**2026-06-10 危害升级实锤**：kakwan S3 被牙膏规则强压成 small，人工定标应为 medium（镜头语言差距）——
过拟合不只污染文案，已实际造成 severity 判定错误。

**Ground truth 已建**：`references/ground-truth-labels.md`（18 阶段全判 + 事实裁决 + 误判账目 + 框架判例）。
回归 = 输入视频 + 该标签，**绝不 diff runs/ 存档输出**（存档全部产自认证修正前代码）。

**重构终态宪法（一句话）**：stabilize 只做一致性修复（severity↔task_completion↔gap 文本矛盾收敛）
和归属搬运；一切品类/商业判断归模型（prompt 框架），代码不再做。
推论：partial 档代码不替模型定级、只查矛盾，禁止新增 "partial→medium" 这类映射规则。

**逐条处置清单（动手时按此执行，不止删牙膏）**：
- S3/S4 牙膏三件套（按压/闻香/功能效果正则）→ 删；
- `creator_not_worse`（中文正则，对马来语 gap 文本失明）→ 改读结构化字段；
- S6 CTA 检测（beli/cart 关键词）→ 待定，按样本检验；
- `has_real_endorsement` 否定正则（临时方案，打过地鼠）→ 被 endorsement tag 取代后删；
- `stabilize_improvement_priorities`、Q11 一致性契约 → 留。

**生死测量（删特例前的 go/no-go 门禁，预注册阈值后再跑）**：
1. 模型在 partial 阶段、品类权重 prompt 下的 severity 稳定性 + 与标签一致率
   （partial 占绝大多数，删特例后 S4 即模型原始判断；此量不过则保留薄兜底）；
2. task_completion 跨 run 稳定性；
3. 品类权重指令对 severity 的执行一致性（与 1 是不同测量目标，删特例前两个都要量）。
方法注意：`--repeat --reuse-existing` 量的是"冻结 facts 的条件方差"，低估生产总方差
（阶段一方差与 Phase C 效应不在内）；预先决定接受条件方差还是花钱测端到端。
阈值开跑前写死（如每阶段众数 ≥4/5、零 large↔small 对跳、标签一致率下限），不许测完再解释。

**子项：第三方背书检测改结构化标记（regex 做不了，规格已定）。**
现有两个 regex 消费者都该换成模型标记：`claims_my.py` 认证归属检测、`repair.py:has_real_endorsement`。
regex 无法表达"机构的数据是否在证明本产品价值"这种关系判断。
2026-06-10 已落地**生产端**：tag 定为 **evidence_unit 级** `third_party_endorsement`（比原计划的
stage/role 级更优：facts 锁定后单一来源、可同时服务认证搬运与 S5 检测、与 product_visible 同模式），
schema/fact prompt/normalize 已接通。**消费端（两处 regex 退役）仍归门禁后执行**——
先在 live run 观察 tag 质量，再切消费者。
**背书定义（已写入 commercial-judgement-framework S5 / prompt / QA Q16）**：
第三方背书 = ① 机构类型（监管认证 / 行业协会 / 评测中心实验室 / 高校研究 / 调研咨询 /
疾病防治中心）+ ② 关联性门槛（该机构的实验/数据/研究在**证明本产品价值**）同时成立；
仅提机构名字、赞助、合作 logo 而无证明本产品价值的数据 ≠ 背书；自述功效 ≠ 背书。

### 2. S4 severity 不稳（中，随 #1 解决）
现状：5 次重复 small/medium 跳变，根因为 #1 那套预存 S4 sensory 规则 × 模型方差。
注意：在 #1 完成前 QA-RULES §10 的 S4 稳定性预期不成立——此不稳是预存 WIP 引入，非分析链改进引入。

### 3. Phase C 回看质量（升级：首次真实执行即产出幻觉，2026-06-10 实证）
原待办（覆盖暗坑）：回看 severity 会被 `stabilize_stage_severity` 二次覆盖——随 #1 终态自然消解
（stabilize 改读 task_completion 后，回看更新 task_completion 即可生效）。
新增（kakwan S6 实证）：Phase C 首次真实触发即幻觉——把无明确 CTA 的达人判为
"明确告知链接在购物车"（实际口播是 "review pun ada dekat background"，whisper 转糊成
rintik/beg kau 提供了错误锚点），将教科书级 large 翻成 small。
确定性触发器标低置信是**对**的，回看反而帮倒忙。要修三处：
1. 回看 system prompt 删除"回看后如果达人持平或更优，必须给 small"的倾向性压力
   （疑似诱导模型为"持平"编造证据）；
2. 回看输出接地约束：声称的口播事实必须能对上转写或标 voice_only，叙事结论受 #6 校验；
3. 输入规范（写入 spec §0）：切片时间窗、缓冲秒数、prompt 告知"边界可能有误差、按功能判断"。

### 4. spec §0：素材包 + 两阶段架构契约（中，开工第一步）
把现状写成契约：预处理产物清单（各轨/帧/标记，谁生成、是否可选、失败如何降级）、
两阶段事实抽取 + video_facts 字段、analysis-output-schema 为字段唯一真相源。
原则：只描述实际存在的东西，不写愿景。好处：挡掉"基于过时结构/臆想决策"的提案。
2026-06-10 追加内容清单：endorsement tag 语义判例（含边界例：自称用三年、评论截图、赞助 logo）、
Phase C 输入规范（见 #3）、stabilize 终态宪法（见 #1）、QA §10 PASS 重定义原则
（4 样本 × 预注册阈值 × 标签一致率；旧 PASS 围绕旧 stabilize + 单一牙膏 run 校准，重构后不适用）、
框架判例五条（见 ground-truth-labels.md 末节，含省钱前提、CTA 过度=硬推、S1/S6 位置先验等）。
各文档（framework/prompt/QA）只引用不复制，spec 为唯一真相源。
顺带重审 prompt"severity 至少出现 2 种"的强制差异化（人为方差源候选）。

### 5. 死代码确认与清理（小）
`prompt.py:render_stage_frame_markdown` 未被 `write_analysis_input` 调用（已有 TODO 标注）；
确认 `artifacts.get_stage_frame_entries` 是否仅服务它。按规范需确认后再删。

### 6. 叙事文本与证据一致性校验（新缺陷类，2026-06-10 双向实证）
gap/summary 叙事字段从不对照转写证据：are_xie S6 假阴性（转写近半是 CTA 却写"视频在有效
CTA 前结束"）/ kakwan S6 假阳性（无 CTA 却写"明确告知购物车"）——同一缺陷类的两个方向。
quote 字段有 `validate_transcript_attribution` 管，叙事文本无人管；`align_timed_cta` 修了
quote 但 gap 不跟着改，产出"证据是一串购买指令、gap 却说没有 CTA"的自相矛盾结果。
方向：轻量一致性检查（如 S6 gap 声称"无 CTA"但 quote 含购买指令 → 警告/重写），
或并入 #1 让 gap 要点由结构化字段生成。

### 7. 观察指引补"镜头语言/取景完整性"维度（小）
歪斜、主体只见局部（kakwan S3 实证：马桶只拍到一边）当前 per-frame 维度抓不到，
却是 S3 执行性差距的有效信号。补进 observation-guide §2 抽帧观察框架。

## 下次开工顺序（2026-06-10 复盘定稿）

1. ~~人工定标 3 样本~~ ✅ → `references/ground-truth-labels.md`；
2. ~~spec §0~~ ✅ → `ARCHITECTURE.md §0`（含判例 / Phase C 输入规范 / 终态宪法 / QA 重定义原则）；
   同批落地：Phase C 三修（#3 前半）、Q19 叙事一致性软警告（#6 v1）、框架判例五条、
   观察指引镜头语言（#7）、endorsement tag 生产端（unit 级）、死代码清理（#5）；
3. ~~生死测量~~ ✅ 已跑（2026-06-11 00:06，15/15 成功）→ **GATE RESULT: NO-GO**
   （runs/_gate/status.log 102-165 行）。T2 挂 7 阶段、T3 对跳 4 处、T4 一致率 10/18、
   T6 2/5、T7 失败。三个 large↔small 错位全部溯源到 2026-06-09/10 的 prompt 判例注入
   （S5 背书内容→全线膨胀；两极逻辑→压掉 tasha S4 真 large；硬推判例→tasha S6 误判 large）。
   **判例是全局校准旋钮，新增判例必须带品类适用条件 + 过 gate 回归。**
   亮点：are_xie S1-S4 稳准、kakwan S4/S6 双中（模型裸判即识破缺 CTA，Phase C 幻觉坐实）。
   待复议：are_xie S6 模型 5/5 稳定 small vs 标签 medium（稳定偏离，需用户再判）。
4. **← 当前位置（NO-GO 后的新路线）**：
   a. ~~修 task_completion 枚举~~ ✅（2026-06-11 fb3b9cc）：prompt 14a/schema/ANALYSIS-PROMPT
      强制三值+达人侧语义；parse.normalize_task_completion shim（38 单测）。
      **回扫结果（16/18 稳定）**：映射后完成度判断 16/18 阶段众数≥4/5（12 个满票），
      vs severity 仅 11/18——"模型事实稳、加权判断飘"论点定量成立，gating 地基恢复。
      唯二不稳均为 S4（are_xie missing/partial、tasha complete/partial）：与 severity 对跳、
      Phase C 动机三线合一，S4=系统真不确定性热点，交回看机制不强求字段层解决。
      遗留：模型 raw 是否真输出枚举（T5 raw 合规）待下次 gate live run 确认（与 4b 共用一轮）。
      标签复议：are_xie S6 medium→small（用户 2026-06-11，T4 基线变 11/18）。
   b. ~~prompt 校准迭代~~ ✅已跑（2026-06-11 round2，15 调用）→ **仍 NO-GO，净倒退**：
      一致率 11/18→9/18、对跳 4→6 处。四靶仅 are_xie S5 方向修正（T7 过但 2/5 不稳）、
      tasha S4 s→m 半步；代价：are_xie S2（信任溢入）/S4、tasha S5（极性 bug 复发：达人更优
      判 large）三处原本正确的被打偏。kakwan S5 划算感归 S2 指令被无视。
      **两轮量化结论：prompt 判例调 severity 不收敛**——每条规则全局扩散，修一伤二。
      raw 层乱、后处理层 kakwan S4/S5 全中——护栏含金量被反向证明。
      极性 bug 系统性（tasha S6 两轮、S5 round2）：模型反复把"达人显著更优"判 large。
      T5 枚举合规仍≈1/5 且词表在膨胀（no_gap/different_focus 新词）——14a 指令基本无效。
   c. stabilize 维持现状不删（known-devil；kakwan S3 误触发已知，由标签记录在案）。
   d. **→ 架构提案（用户已采纳方向，2026-06-11）**：severity 改为从稳定事实层确定性推导，
      不再由模型直出。用户从业务侧独立梳理出同构设计（V2.1：E×L 公式+低置信自检），三条
      定稿原则：① E 由代码从稳定事实推导，模型不直出；③ 品类痛点清单进权重表（数据），
      模型只报卖点事实，命中查表；④ 事实不支撑则不判断（S5 双方无真背书→均未涉及）。
      **离线校验已完成（scripts/dev_derive_severity.py，零 LLM 成本，对两轮门禁 30 份 raw）**：
      一致率 round1 14/18、round2 16/18（模型直判基线 11/18、9/18），对跳 6→1。
      round2 过 T4 预注册线（≥15/18）。剩余 4 个失分**全部记账到事实层**：
      kakwan S3（镜头语言提取盲区→observation-guide）、kakwan S6（2026-06-11 母语同事三遍
      重听终裁：达人确有一句极弱敷衍 CTA——模型听觉平反，缺的是强度分级；已加 E=1.5 敷衍档，
      medium vs large 邻级差留给权重数据积累去填，用户裁决不现在拧）、r1 时代 are_xie S4/
      kakwan S2 事实文本不稳（r2 已自愈）。推导层零失分。
      用户三裁决（2026-06-11）：弱 CTA 敷衍档 E≈1.5；清真=马来市场准入基线不构成差异化背书
      （判例已入 commercial-judgement-framework.md）；权重表靠后续对比数据积累渐进优化，不一次定死。
      管线落地需新增事实字段（替换离线代理）：gap_direction 枚举（替代 severity 二值化代理）、
      S4 verification_mode（动作演示/口头宣称——tasha S4 实测方向词五次两极漂移而画面事实
      五次一致，必须锚定观察事实）、背书层级（权威认证 vs 清真等基线合规——判例待用户确认）、
      钩子痛点命中。L_link×L_product 标量不够表达，已合并为品类×阶段权重表 W。
      ⚠️ 诚实声明：权重 W 在同一批 18 标签上调出，此为可行性上限证明非泛化验证，
      需新样本做出样本外测试。
      注：此为终态宪法的修订——"商业判断归 prompt"两轮实测不成立，判断政策应为代码中的
      品类权重表，模型只供事实。
   e. **管线落地 ✅（2026-06-11）**：阶段 2 新增两侧独立执行分 creator/benchmark_execution
      （0/0.5/1/2，先打分再对比）+ 顶层 category_profile（模型报品类事实，权重政策在代码）；
      新建 postprocess/derive.py（原型 W 表 + E=标杆−达人 + S5 背书门槛/S4 演示差分/S1 痛点
      差分 + 红线 + 逐阶段算法溯源 severity_derivation），挂在 stabilize_stage_severity 之后、
      improvement 重排之前；字段缺失/异常优雅降级保留模型 severity（存量数据回放验证不变）。
      ~~下一步：带新字段重跑门禁验证执行分稳定性~~ ✅已跑（2026-06-11 round3，15 调用+2 废件）。
   f. **round3 结果（执行分实弹验收）**：
      - 途中破案并修复：stage2 payload 的"输出要求"字段清单硬编码、不读 analysis_input.md——
        14b/14c 起初没进请求体（烧 2 次发现止损）；同时解释了 T5 悬案（14a 从没送达过 stage2，
        真送达后 task_completion 枚举立即全规范）。
      - **执行分事实层很稳**：90 个单侧打分序列绝大多数 5/5 全同或单步摆动（0.5↔1），
        方向翻转全程仅 1 处；E 的极性事故结构性消失。
      - 双口径：实际推导 9/18（severe 2、对跳 1）；**痛点命中 oracle 口径 15/18、severe 0**
        ——失分 6 个全在 C 系数词法匹配（模型痛点词与 gap 文本语言/粒度错位，分词修复仍不可靠），
        3 个在事实层。模型直判本轮 ≈11/18 且 tasha S5/S6 仍有 l↔s 极性对跳。
      - **机器纠偏实录**：kakwan S5 模型直判 large×5（划算感误归背书第三轮复发）→ S5 背书门槛
        全部拦截纠正 small×5 正中标签；tasha S6 模型 large×3（极性 bug）→ 推导 small×5 ✓。
      - derive.py 两修：原型选择加 price_tier=low+functional→冲动品规则（模型 decision_threshold
        对马桶刷 4:1 摆，price_tier 稳）；painpoints 复合串分词。
   g. ~~下一迭代~~ ✅三项已落（2026-06-11，commit eac6b36）：① painpoint_relevance 枚举字段
      （C 去词法化）；② 执行分 0.5 锚点扩义；③ 镜头语言进 Phase A。存量重放 severe 全归零。
      另从 TabAI"六通道有效性"文档（references/fact-effectiveness-channels.md）吸收四项：
      孤证规则（S5 口播提及无画面佐证→执行分≤0.5）、"无法有效接收→0.5"原则（看不清/听不清/
      读不完）、UI 危险区+遮挡观察维度（Phase A + observation-guide）；伪精度数字（10度/0.3秒/
      confidence阈值）与六通道全量 checklist 拒收（fps3 测不了 + 指令稀释实测教训）。
   h. **round4 终验收（2026-06-11 晚，16 调用含 1 次补打）：实跑 15/18，severe 0——过 T4
      预注册线（≥15/18），prompt 两轮、模型直判四轮从未达到的水位**。
      分项：are_xie 6/6（S1 达人 0.5×5 全同、S4 锚点扩义治愈、S6 缺字段降级回退实弹验证）、
      kakwan 4/6（S1 ✓锚点生效；S6 ✓经 C 作用域修正——painpoint 系数与促单功能正交，
      S6 免除 C 调制，已修 derive.py）、tasha 5/6（S4 large ✓含演示差分）。
      同批模型直判 12/18 + severe 2：kakwan S5 划算感→large 第四轮复发、tasha S6 极性第四轮
      复发，均被推导层机械纠正——模型偏见仍在但已伤不到输出。
      残差 3：kakwan S2（掉毛对比缺失未入事实，执行分平局）、kakwan S3（镜头语言；Phase A
      facts 缓存复用，4g③ 修复**本轮未被测到**，要测需每样本 +2 次调用重抽事实）、
      tasha S1（S=1.2 恰好压 small/medium 阈值线，连续两轮；标尺边界问题留数据积累裁决）。
      其他：休眠中断一次（curl 自愈+skip-existing 补打 raw_04）；painpoint_relevance 枚举
      稳定可用；执行分序列绝大多数 5/5 或单步摆动。
   i. 任务3 ✅（2026-06-12 晨，事实重抽验证）：dev_refresh_facts.py 重抽 kakwan Phase A
      （2 调用，旧 facts 归档 facts_backup_*），新观察维度激活（6 处镜头语言/遮挡记录，
      多为阴性"无遮挡"——与人工定标"歪斜/只见一边"有出入，可能抽帧未覆盖）。
      新facts下 stage2 重跑：kakwan 4/6→5/6（S2 掉毛对比入事实→medium ✓；S3 标杆执行分
      2/4 次拉开到 2，僵局松动但未过半）。滚动总分 16/18 severe 0。
      夜间 API 中断致 1 index 三连败，skip-existing 补打。
   j. **Phase C 升级（部分完成）**：临界分值触发+优先级排序已落地（commit b4ffd62）；
      ⚠️ code review 发现评审模板缺执行分字段→复核后无法重推导（见 4k 修复清单）。
   k. **Phase C 升级（待做，代码侧）**：回看触发加优先级排序（P1 S1/S6 低置信必看、
      P2 S4 声画冲突）+ **临界分值触发**——derive 的 S 落在 large 阈值邻域（如 2.3-2.7）时
      送 Phase C 复核事实再重推导。S 确定性化之后这才变得可行（4d 红利）。
      字幕停留时长可由 subtitle_track.json 时间戳确定性计算——有失分实证再建（先测后做）。
   l. **round5 样本外盲测（2026-06-12，7 对全新样本：泰语×2 + 马来语×5 含无口播×2，
      规则冻结于 round4 终态）**：推导 **23/42** vs 模型直判 21/42（样本内 89% → 样本外 55%，
      真实泛化水位）。泛化成立：S5 不涉及门槛 **7/7**、S1 痛点差分 5/7（youkoubo-c0
      "谢谢观看"无痛点→large 命中）、severe 4 个全部中段低估**零极性反转**、无口播（whisper
      BGM 幻觉）/泰语/休眠重启全部优雅存活。失分收敛单一缺口：**S2-S4 仅 6/21——执行分
      标尺缺"呈现有效性"轴**（skincare S2 判达人占优 c2/b1 方向反转；youkoubo-c2 糊弄视频
      S3 给 1 分平局）。存疑对 2（泰语 whisper S6，用户自注不确定，待原生重听）。
      42 标签入 ground-truth-labels.md，离线重拟合语料 18→60。
   m. **下一步候选：执行分标尺加"呈现有效性"轴**（2026-06-12 用户两判例修正锚点定义，
      详见 ground-truth-labels.md round5 节 + 框架 S3/S4 判例）：
      2 = 关键环节可视化到位 + 效果突出有对比 + 场景丰富（**不奖励过长全程记录**——
      标尺是关键环节覆盖非完整性）；
      1 = 过程有但关键环节或效果对比缺一、场景单一；
      0.5 = 过程缺失仅口头/文字，或**画面晃动到观众抓不住重点**（无法有效接收，
      观众可看性视角非镜头美学）。
      落地路径：改 payload.py 执行分指令（真相递达点）→ 60 标签离线重放估增益 → 重跑验证。
   n. **呈现有效性轴落地 + 任务5 权重首拟合 ✅（2026-06-12 下午）**：
      锚点重跑（7 样本×1，与基线同口径）：推导 20/42 表面倒退，但拆层后——锚点对模型生效
      （直判 21→23，skincare S2 c2/b1 方向反转治愈、达人 0.5/标杆 2），瓶颈移到政策层：
      W 表把 E=2 压在 medium（wukoubo-c0 S3 S=2.4 差 0.1 过线，模型自己都判 large）。
      → dev_refit_weights.py 任务5 首跑：60 标签坐标上升（S1 锁框架红线"Hook 不降权"，
      防住了优化器在 n=1 感官样本上找到的 S1=0.6 堕落解），36/60→**40/60，severe 7→5**；
      变化可解释：理性 S3 1.0→1.2、感官 S2/S3 1.0→1.6（中段呈现权重上调），已应用进
      ARCHETYPE_W。**校准循环全程零 LLM 成本——4d 承诺的兑现。**
      剩余失分主因：①round5 全部单跑、执行分无众数平滑（round4 实证有±1 步摆动）；
      ②糊弄视频的晃动未被识别（youkoubo-c1 S1 给 2 分）；③skincare S4 模型给 c2/b2 平局
      （事实层分歧）。
   o. **晃动确定性信号 ✅（2026-06-12，选项 b）**：flayr_core/motion.py 用 vmafmotion
      （320px/6fps 全片均值）测晃动，阈值 22.0 经验校准（12 条视频：糊弄组 22.9/26.4 vs
      正常组≤19.0 含快剪标杆，**20 条视频零假阳性**）；预处理挂载 analysis.videos[role].shake，
      derive 对 severe 侧 S1-S4 执行分封顶 0.5（只降不升/双侧对称/无信号优雅照旧）。
      晃动独立 40→43/60 severe 5→2；叠二次拟合（理性 S2/S3→1.4）后 **45/60 severe 2**。
      今日累计 36→45/60（60%→75%），severe 7→2，除 7 次锚点重跑外全部零 LLM 成本。
      ~~剩余候选 a~~ ✅见 p；c) 新样本做拟合后真泛化验证（待用户供片）。
   p. **选项 a：repeat5 稳定口径 ✅（2026-06-12 晚发射，28 调用过夜跑完）**：
      稳定口径（众数）43/60 → 三次拟合（理性 S3 1.4→1.6）后 **44/60 severe 2**（73%）。
      单跑 45 vs 稳定 43：单跑噪声净虚高 2 分，幅度可控。
      分项：youkoubo-c1 6/6、youkoubo-c0 5/6、skincare 4/6、youkoubo-c2 4/6、
      wukoubo-c1 3/6、bluetoothwanju 3/6、wukoubo-c0 2/6（+原三样本 16/18）。
      severe 2 根因查明：**标杆侧执行分跨对摆动**——同一标杆视频在 c0 对 b=2×5 全稳，
      在糊弄达人 c2 对 b 摆 1↔2（达人内容污染标杆打分的锚定效应，违反独立打分纪律）。
      **新候选机制（零成本）：同一标杆视频执行分跨对共享/池化**——标杆侧打分本就与达人
      无关，按独立打分纪律池化既治锚定又省方差。
      残差主模式已裁决（2026-06-13 用户选②）：**"都做了但呈现单薄"上限 medium 判例**
      入框架核心标尺；wukoubo-c0/c1 S2-S4 标签复议 large→medium。复议后稳定口径
      **48/60（80%）severe 2**，权重表无需再动——推导数学结构（E=1×W≤1.92<2.5）与判例
      天然一致，用户标尺与代码标尺完成对齐。剩余 severe 2 = youkoubo-c2 标杆侧执行分
      跨对摆动（池化机制候选，见上）。**当前状态：等用户供 c 阶段新样本做真泛化验证。**
5. 重测过门禁后 → 按 #1 处置清单执行；验收按新 QA 定义，回归只对"输入 + 标签"。

## 观察后再决定（先测后做，目前数据不支持建）

- **creator_script 照搬标杆口播检查**：实测 5 条最高相似度 0.40，未发生照搬。
  若将来真观察到，做**确定性代码校验**（非 prompt 自检），仅同语言射程。
- **aigc_prompt 对锚帧的接地检查**：实测接地良好（仅 C5"购物袋"轻微外引）；
  且重合度判不了接地（改造 vs 白描天然低重合）。真要做须语义手段，非正则。

- **price_tier 来源升级 ✅（2026-06-13）+ 第二层冗余假设（低优观察）**：
  第一层已落地——price_tier 来源从"模型世界知识猜"改为"运营领域知识供"：
  `--product-tier`(low/mid/high) + `--product-price` 留痕 + derive `_reconcile_operator_tier`
  降级链（运营 > model_fallback）+ schema/框架判定口径文档。运营值覆盖、推导不变、
  降级兜底、无 analysis 优雅，5 项验证通过；全样本零回归（48/60 不变）。
  **第二层（price_tier 影响权重）从"待开发"证伪为"结构性冗余假设"**：6 高价样本
  （are_xie + poboji ×5，已标 operator high 入语料）在 considered 分支推导零变化——
  decision_threshold 已接走"决策有多重"。唯一非冗余点：`_select_archetype` 第一行
  `impulse→impulse_low_price` 隐含 impulse=低价；**已加触发器 `impulse+high → archetype_warning`**
  （运营档位驱动，零阈值逻辑）。该组合受 Flayr 选品场景 + 模型判断习惯双重过滤，
  结构性罕见。**结论：不立 high 独立原型；触发器命中前不动权重；待告警出现再议。**
  待确认（产品侧）：Flayr 核心服务场景是否系统性排除"高价+impulse"——若是，冗余结论可提前锁定。

## 已否决的提案（避免重复提）

- **执行分细化到 5 档（加 1.5"有创新"）→ 已实验否决（2026-06-13，15 调用，已回滚）**：
  carslan-b0/colorkey-b0/mmx repeat5 实测。① 1.5 档模型愿意用（递增 13%→26%→35%，均 25%）、
  稳定性也没崩（S4 全 5/5 全同），两个担心的风险都没发生；② **但 S4 medium 命中 0/3，与 4 档
  完全持平、零收益**——根因是模型 S4 赋分的**方向/两极**（carslan 达人打 2 偏高→small、
  colorkey 达人打 0.5 偏低→large，都偏离用户"1 合格但单薄"），不是档位粒度。细化档位给了模型
  新刻度，治不了它对 S4"中间质量"的赋分能力。**S4 真问题 = 模型赋分方向，留作"赋分层"专项**
  （与人自一致测量的 S3/S4 内在模糊区呼应：人机在难判处共同不稳）。5 档 raw 归档于各样本
  gate_5tier_experiment/。**结论：细化档位可行但解错了问题，回滚；4 档基线 48/60 不动。**

- **人标签自一致性测量（2026-06-13，方法论基线）**：用户盲重标 2 样本 12 阶段，自一致
  **10/12=83%（剔泰语 S6 存疑项 = 91%）**。carslan（几小时）6/6 全稳，bluetoothwanju（几天）
  S3 真漂移（S3/S4 边界模糊）+ S6 存疑。**机器对齐天花板 ≈ 85% 非 100%**：机器样本外 ~56%，
  真实差距 ~30% 非 ~44%；漂移点（S3/S4 边界、泰语听不清）= 机器失分点 = 内在模糊区。
  机器合理目标 = 逼近 75-80%（接近人自一致上限），追 90%+ 是追人自己都到不了的幻影。

- **S4 赋分降维（IU 清点 + proof_strength → 代码推执行分）→ 已实验否决（2026-06-13，15 调用，已 revert）**：
  carslan/colorkey/mmx repeat5。S4 medium 命中 **0/3，与 4 档持平、零收益 + 增复杂度**。
  挖到的最深根因（本次实验的真价值）：**模型对"S4 有效效果证据"的视觉理解，和用户系统性不一致**。
  ① IU 布尔方向三样本无一稳定对（carslan 达人 IU3>标杆1 反、mmx 达人 IU2>标杆1 反、colorkey 持平）——
  连家电 mmx（使用过程视觉具体）都反，不是美妆特例；② proof_strength 系统偏差（carslan/mmx
  达人永远 strong、标杆永远 moderate）。**模型把"达人做了使用动作/对比构图"当效果证据强，
  用户要"产品真实效果被有效呈现"——模型在视觉层混了"做了动作"(S3) 和"展示了效果"(S4)**。
  降维（IU/proof）触不到此根因：问题在"有效性的定义"不一致，不在标尺/档位/清点形式。
  **与人自一致测量闭环**：S4 是人机共同模糊区（用户自己 S3/S4 边界都漂），S4 的"有效"本身
  就是模糊判断。**这很可能是模型视觉理解的能力边界。**
  改进方向待用户定：a) 接受 S4 为已知能力边界，维持现状靠 Phase C 人工复核；b) 品类 EP 库
  （给每品类定义"有效效果证据"，工程大且根因存疑——EP 库也得模型能识别品类效果）；c) 其他。
  实验 raw 归档于各样本 gate_s4_experiment/。

- **S4 类型选择层（s4_effect_type + effect_visibility）→ 已实验否决（2026-06-13，15 调用）**：
  back-to-basic 转向后的最小验证（从"达人vs标杆相对差"补"达人离品的阶段最佳"，强制区分
  S3 使用动作 vs S4 效果呈现）。达人 none 命中：carslan 0/5、colorkey 4/5、mmx 0/5 = **1/3 准**。
  **mmx（家电、效果最具体）也撞墙**→不是美妆抽象特例。精确边界：**只要画面有"效果呈现的
  视觉形式"（前后场景/对比构图/特写），模型就判有效 type，不管实质；只有连形式都没有
  （colorkey 纯介绍）才判 none**。模型分不清"形式 vs 实质"，在"看"这一层，加字段补不上。
  待用户定：回滚 / 保留字段接推导（仅 1/3 准，价值低）/ 接受 S4 能力边界。effect_visibility
  carslan 全没输出（指令稀释）。

- **【S4 三次实验收敛结论，2026-06-13】**：5档（赋分两极）/ IU清点（动作当证据）/ 类型选择
  （形式当实质）三个角度撞同一堵墙，根因确定：**模型分不清"S4 效果呈现的形式与实质"**——
  看到对比构图/前后场景/动作就判"有效呈现"，判不出"走过场 vs 真展示效果"。这是视觉理解
  能力边界，不在任何可加字段层（执行分/IU/类型）。与人自一致测量闭环（S4 是人机共同模糊区，
  用户自己 S3/S4 边界都漂）。**务实方向：S4 定为系统已知软肋——报告标低置信 + Phase C 人工
  复核兜底，不再投"加字段"方向（三次已证无效）。** S4 对齐天花板 < 其他阶段，是问题性质决定的。

- **类型选择层（s4_effect_type + effect_visibility）→ 已实验+回滚（2026-06-13）**：
  达人 none 命中 carslan 0/5、colorkey 4/5、mmx 0/5。**用户复看视频后翻转结论**：模型
  s4_effect_type 识别其实是对的（达人确有 before/after，只是弱），是标签凭整体印象错了
  （把"做了但弱"看成"没做"）。元教训：模型≠标签不等于模型错。字段 1/3 准+未接推导，已回滚。

- **【S4/执行质量探索的最终收束，2026-06-13 深夜】**：今日四个实验（5档/IU清点/类型层/阈值搜索）
  把"S4 老大难"诊断到底，结论分两层：
  ① **类型选择**（达人用了什么效果呈现打法）模型能判准——三次"撞墙"实为标签错（用户复看翻案）。
  ② **类型内执行质量**（对比强不强/节奏/突出度）是真问题，且**全阶段非 S4 独有**。
  执行质量高估的精确诊断（对存量 78 标签统计）：E=标杆−达人 方向正确且单调
  （large 1.09>medium 0.67>small 0.02），**但模型中庸打分**——不敢用满 0-2 动态范围
  （该 0 打 0.5/1、该 2 打 1.8），导致 E 被压缩、large/medium 的 S 分布严重重叠
  （large[0.84,3.84] vs medium[1.20,3.84]）。**阈值网格搜索证实 1.2/2.5 已最优、调不动**
  （S 重叠是上游 E 可分性问题，下游阈值救不了）。
  **唯一出路（待验证，非零成本）：执行分"极化打分"指令**——差距大时鼓励达人敢打 0、标杆敢打 2，
  把 E 动态范围拉开。这是改指令+重跑，明日清醒再开。当前 48/60 基线稳。

- **【产品分析层落地 + carslan S4 个案收束，2026-06-15】**：
  ① **执行分"上撑"实验否决**：挤窄 1/松 2 门槛，3 样本小验 15/18→16/18，但 +1 在噪声内——
     真正变好的（skincare S2、wukoubo S1）执行分老新完全一样＝category_profile 档位漂移撞对，
     规则真驱动的 S4 反被搅离标签。根因诊断错：large 对标杆执行分本就封顶 2，上撑无向上空间。已回滚。
  ② **产品分析层 product_profile 落地（commit c525216）＝主线进步**：判分前模型先立"本品视觉命题"
     尺子（core_visual_proposition + visual_diff_dimensions + trust_multipliers + shooting_requirement），
     再对照打分；逐阶 proposition_delivery 事实（先收集不卡分）；核心命题为主轴、次要演示不补偿弱核心（用户裁断 14b5）。
     验证（carslan/colorkey 只供品名+品类、命题让模型推）：命题推导可靠泛化（油光→哑光 / 玻璃肌全推对）；
     **干净 gap 判准**（colorkey S4 benchmark_only / C=0.5 B=2 / large，完全合人眼）。纯增量、48/60 不回归。
  ③ **carslan S4 ＝ known-limitation，最小化处理（park）**：达人 C=2 不降，9/9（命题锚定/闭环核验/核心分离
     三版 prompt × 三次）模型把弱核心感知成"已呈现"，prompt 修不动——"看不清图非 prompt 可解"再次实锤。
     CV 探针**零成本双证伪**：整图半脸高光比方向反（手指+姿态污染）；脸颊隔离仍被反光美甲指尖+鼻翼污染、
     正脸检测在 3/4 侧角直接失败、相邻帧跳 50pp ＝纯噪声。要破需重型 face-aware CV，且这帧达人手指全程
     戳在 After 颊上、遮挡无解。**判定**：carslan 是多小概率因素凑齐的对抗性个案（达人多次验证 + 效果本就
     细微 + 复杂现场光 + before/after 与纸巾拍脸双验证 + 恰好粉饼品），为偶发坎做大范围改动不划算。
     待品类积累够再说。S4 仍走"低置信标注 + Phase C 人工兜底"。**方向（产品 DNA 锚定）是对的、有效的，主线继续。**

- **【六阶段框架落地 + 分歧 triage #1，2026-06-15】**：S1-S6 产品锚定框架全部 commit（c525216/3769199/45530f5），
  S5/S6 + S1/S2/S3 各做了小样本验证。验证结论 + 今日 model-vs-user 分歧的 type 分类：
  ① **S2 纠正后 3/3 干净**（卸下卖点、改判"引出自然+承接钩子"），无误伤。
  ② **S5/S6 方向 8/8 对**；colorkey S5 软信号 → 已规则化（软硬背书梯度 + 位置宪法，commit e642e6e）；
     mmx/bluetooth S6 偏狠一档但用户已认可模型（非分歧，盲标偏糙）。
  ③ **S1/S3 系统性"判反"——但 triage 后定性为架构限制、非规则缺口，不入库**：
     carslan S1/S3、bluetooth S3、mmx S3 全是模型孤立打分**偏袒达人**（奖励"达人讲得多/文字对比/教学详尽"
     over "标杆真实演示"）。定稿规则（演示即证据 / 不评教学清晰度 / 场景丰富性）本就对，**模型没应用**——
     这是**参照系缺失（孤立绝对打分丢了对比框架）**，加规则治不了（同 S4 的 9/9 prompt 无效）。复现闸真相：
     递归出的是同一条已有规则（演示>讲述），不是新规则 → **不入库，避免 bloat**。
  ④ mmx S1 模型反向偏狠达人（C=0.5，惩罚"痛点描述开场弱"），待用户复看裁"偏狠 vs 标签糙"。
  **元结论**：规则库当前不缺规则，缺的是"规则进不进得了模型判断"——下一步 task 2（manipulation check：
  同达人配强锚 vs 弱锚看 E 漂不漂）验证这件事，再决定要不要为"累积规则库"大规模投入。
  架构反转（独立绝对打分 → 盲化比较出差距，benchmark/锚当外置参照系；E 定为相对量）已方向收敛，待清醒做。

- **【manipulation check #1：保险丝通过，比较 pivot 绿灯，2026-06-15】**：同一条 carslan 达人，
  分别配 b0 标杆（标杆视频.mp4）vs b1 标杆（标杆视频1.mp4），group2 prompt 各 ×3，看达人侧执行分漂不漂。
  结论：**标杆确实进了模型对达人的判断（达人 C 随标杆变），且方向正确**——
  S1：b0 标杆弱(B=[1,1,1]) → 达人 C=[2,2,2]；b1 标杆强(B=[2,2,2]) → 达人 C=[1,1,0.5]。
  **强参照 → 达人显得更弱**，教科书级正确反应。S5/S6 也漂（方向未逐一核），S2 不动、S3/S4 轻漂。
  三点验证：① 标杆进判断（漂移=实锤，"独立打分禁回调"其实没被遵守，系统本就隐式比较只是没控）；
  ② "好是相对参照的"（同达人配弱标杆显好、配强标杆显弱）；③ **解开 carslan S1 判反之谜**——
  不是模型瞎，是 b0 标杆本身 S1 弱(B=1)、达人相对它显得好(C=2)，而用户"large"标的是脑中强 S1 标准
  ≠ b0 实际弱 S1（实锤"n=1 标杆 ≠ 内部分布"）。**意义**：保险丝问题"规则/锚进不进模型判断"答案=进且对，
  → 显式比较 + 喂更好参照（累积规则库/更强锚逼近内部分布）会奏效，架构反转从"理论该做"变"实测有底气"。
  边界：n=1（一达人一次换标杆），漂移有噪声，需更多样本固化；但最干净的 S1 给了明确正向信号。

- **【盲化成对比较 POC #1：full content（帧+口播）扳正判反，比较 pivot 硬背书，2026-06-15】**：
  新建 `scripts/dev_blind_compare.py`——取某阶段达人/标杆的帧+口播，盲化为视频A/B（不告诉谁是标杆），
  只在"本阶段产品到位标准"上问谁更好+gap，双序各跑一遍控位置偏置。在 S1/S3 判反 case 上测：
  - **纯帧版**：carslan/mmx S3 仍选达人（被达人显著的 Before/After 叠层骗），bluetooth flip → 没扳正。
  - **加口播后（用户洞见：口播暴露啰唆/低密度/卖点没讲清）**：**carslan S3 双序一致翻成"标杆 large"，完全对上用户标签**
    （达人 S3 口播="无意义感叹"完全没传卖点，标杆口播清晰罗列控油/遮瑕/锁妆）——**之前的判反是没喂口播的测试缺陷，不是感知天花板**。
  - 双序置信机制有效：bluetooth S3（接近案）、mmx S3 标低置信，**mmx 还自己查出产品错配**（"画面地毯清洗机但口播提喷嘴出水严重错位"）。
  - carslan S1 仍选达人，但 b0 标杆 S1 本就弱（manip check 实锤 B=1），对着弱标杆判"达人痛点开场≥囤货口号"成立 = n=1 标杆问题非模型错。
  **结论**：full content（帧+口播）+ 盲化 + 双序，在最清晰判反上扳正、模糊案正确低置信 → **比较 pivot 有硬实测背书**。
  下一步：把 compare pass 接进管线（盲化+双序+锚到位标准+full content，E 定相对量；绝对分降级为水平/交叉校验出置信度）。harness 已 commit。

- **【overclaim 校正 + 口播结构 + 诚实账本，2026-06-15】**：外部两份分析抓出我前两个 commit（manip check / 盲化 POC）的 overclaim，逐条 walk back：
  ① **"解开 carslan 判反之谜"下早了（循环验证）**：用"模型给 b0 标杆打的 B=1"去证"模型给达人打的 C=2 没错"=用模型验模型，
     只证了"分随锚动"，没证 b0 的 S1 **真**弱。**待用户亲眼看 b0 S1 才能裁**（真弱→reframe 成立；不弱→模型误判标杆，reframe 塌）。
  ② **阶段对不上**：最初判反是 S4（带感知 confound），manip check 干净信号是 S1（纯参照系、无感知）；S1 漂亮恰因无感知 confound，不能外推解释 S4。
  ③ **existence proof ≠ reliability proof**：干净的只 S1 一格（S2 没动、S3/S4 轻漂、S5/S6 方向没核、S6 [2,0.5,2] 满量程=噪声）。绿灯=可以去建+做真验证，非已验证。
  ④ **S2 [1,1,1]→[1,1,1] 是隐藏疑点**：要么 do-no-harm（好），要么锚根本没进 S2 判断（保险丝失效藏在绿灯里）——待对用户 S2 判断核。
  ⑤ **旧架构回溯警告**："独立打分禁回调"从没被遵守、标杆一直渗 → 过去所有"绝对分"本质是被上下文标杆污染的隐式比较分，地基性影响。
  **盲化比较 POC 的正面也守纪律说**：carslan S3（口播扳正，ASR 已验真：达人 S3 口播=泰语"哇哦哇哦"纯感叹）、S4（扳正，但靠"动态实测 vs 静态叠层"可感知粗差异，**非细微油光感知**——细微感知没被攻克，只被证"很少是决定性票"）。bluetooth/mmx S3 正确低置信。
  **口播该有的结构（Doc 1，建证据流时用）**：口播=贯穿 S1-S6 的转写证据源、**不开 S7**；劈两层——语义层（信息密度/卖点覆盖/声画一致/叙事结构，文本=模型强=可自动）vs 副语言层（念稿感/语速情绪/感染力，转写即丢=感知天花板=先收集不卡分）；**新瓶颈=ASR 转写质量**（东南亚多语+BGM 易 garbage-in，判前先验转写）；S3 不被口播推翻主轴——口播给 S3 加"声画一致性"交叉尺，且压住"口播提到≠卖点传到位"的音频通道通胀。
  **待办（要用户的眼/单独实验）**：a) 用户核 b0 S1 真伪；b) S2 [1,1,1] 对账；c) 给 S4 补独立 manipulation（带感知 confound 的案发现场）；d) 跨样本固化 reliability。

- 本地 CV 产品轨 `product_track.json`：无参考图、首帧定模板对达人视频失效 → 已改寄生 evidence_units。
- 音频分轨 / 人声分离：VidLingo 已验证不划算。
- 音频质量探针：对短视频分析无价值。
- 轻量 VL 替代主模型抽取：打碎 video+audio 跨模态融合，未必更省。
- evidence_unit 加 `stage_hint`：违反"看功能不看模式"宪法 + 锚定放大漂移 + 省的成本不存在。
- evidence_unit 加 `voiceover_tone`：信号已在 `audio_fact`；评价部分属阶段二 `voice_performance`。
- evidence_unit 加 `representative_frame_uri`：时间→帧已由代码确定性解析，模型出 URI 有幻觉风险。
- 商业权重第 0 步移到最后 / severity 对权重失明：与"品类权重进 severity"的既定决定冲突；
  可审计性已由 `task_completion`(工艺) vs `severity`(影响) 双信号在后台满足。
